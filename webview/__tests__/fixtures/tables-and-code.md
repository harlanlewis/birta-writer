# Tables and code

| Feature    | Status      | Notes                    |
| ---------- | ----------- | ------------------------ |
| Tables     | done        | drag handles             |
| Code       | done        | language picker          |
| Math       | planned     |                          |

Aligned table:

| Left | Center | Right |
|:-----|:------:|------:|
| a    | b      | c     |

```typescript
const x: number = 42;
function greet(name: string): string {
    return `hello ${name}`;
}
```

    legacy indented code block
    second line

Inline `code with * and _ chars` stays literal.
