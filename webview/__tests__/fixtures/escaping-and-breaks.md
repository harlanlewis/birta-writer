# Escaping and hard breaks

Special chars in prose: 5 * 3 = 15, snake_case_name, array[index], a < b.

Line one ends with two trailing spaces  
line two continues the same paragraph.

Windows path C:\Users\test and a literal backslash \* escape.

Price is $5 and $10 — dollar signs, not math.
