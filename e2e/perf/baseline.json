{
  "note": "Holds NO measurements, deliberately. This file gates nothing and no code reads it — the loop gates on a same-session A/B (`pnpm perf --compare`, `pnpm perf:ab`). Figures kept here could only be quoted by a human, and went stale the moment anything landed while still reading like a current figure. Rebuild and run `pnpm perf` for current numbers. Guarded by bundleBaseline.test.mjs.",
  "lastLanded": "MAR-67 (extract sampleDocPosition; selectionToolbar leaves the bundle)",
  "build": "production (node esbuild.mjs --production --metafile)",
  "runsPerFixture": 10,
  "aggregation": "median of runs 2..10 (first discarded)"
}
